## What & why

## Checklist
- [ ] `nix flake check` passes (shellcheck + module eval + `treefmt`)
- [ ] `nix fmt` run (treefmt: nixfmt + deadnix + statix)
- [ ] No secret values/names added; every-shell model + threat-model docs intact
